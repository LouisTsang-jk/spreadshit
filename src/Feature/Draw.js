// new Draw(ctx,x,y)
// draw

// input ctx, x, y, width, height,style

// text-align left,center,right
// color, background-color
// font-size
// pattern hidden,visible,wrap

// > 可能会影响该行最低高度(如果绘制高度>单元格高度)
// > micro render queue有些渲染需要整体渲染完成再渲染，否则对其他单元格会有影响