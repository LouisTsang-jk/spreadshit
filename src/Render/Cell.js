/**
 * draw cell
 */
export default class Cell {
  constructor (x, y, width, height) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    // style: background,text-align,overflow
  }

}